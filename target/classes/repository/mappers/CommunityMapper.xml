<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springstudy.bbs.mapper.CommunityMapper" >

	<select id="CommunityBoardList" resultType="Community" parameterType="hashmap">
		SELECT 
			no, 
			title, 
			nickName, 
			content, 
			regdate,
			readcount,
			recommend,
			medic
			
		FROM community
		<where>	
			<if test="type == 'title'">
				title LIKE CONCAT('%', #{keyword}, '%')
			</if>
			<if test="type == 'nickName'">
				nickName LIKE CONCAT('%', #{keyword}, '%')
			</if>
			<if test="type == 'content'">
				content LIKE CONCAT('%', #{keyword}, '%')
			</if>	
		</where>
		
		<choose>
			<when test="category == free">
				 AND category='board'
			</when>
			
			<when test="category == qna">
				 AND category='qna'
			</when>
			
			<when test="category == chuchu">
				 AND category='chuchu'
			</when>
		</choose>
		
		
		
		
		ORDER BY no DESC
		LIMIT #{startRow}, #{num}
	</select>

	<select id="getCommunityBoardCount" parameterType="hashmap" resultType="int">
		SELECT 
			COUNT(no) 
		FROM community
		<where>	
			<if test="type == 'title'">
				title LIKE CONCAT('%', #{keyword}, '%')
			</if>
			<if test="type == 'nickName'">
				nickName LIKE CONCAT('%', #{keyword}, '%')
			</if>
			<if test="type == 'content'">
				content LIKE CONCAT('%', #{keyword}, '%')
			</if>	
		</where>	
	</select>
	
	<select id="getCommunityBoardDetail" resultType="Community" >
		SELECT 
			* 
		FROM community 
		WHERE no = #{no}
	</select>
	
		<select id="replyList" resultType="Reply">
		SELECT 
			no,
			bbs_no AS bbsNo,
			reply_content AS replyContent,
			replynk,
			replydate
		FROM reply
		WHERE bbs_no = #{no}
			ORDER BY no DESC
	</select>
	
	<update id="incrementReadCount">
		UPDATE community
			SET readcount = readcount + 1
		WHERE no = #{no}
	</update>
	
	
		<insert id="insertCommunityBoard" parameterType="Community"
		useGeneratedKeys="true" keyProperty="no">
		INSERT INTO community(title, nickName, content, medic,regdate, readcount, category) 
				VALUES(#{title}, #{nickName}, #{content}, #{medic}, DEFAULT, 0, #{category})
	</insert>
	
	<update id="updateBoard" parameterType="Board">
		UPDATE community SET title = #{title}, content = #{content},
				regdate = Now()
		WHERE no = #{no}
	</update>
	
	<delete id="deleteBoard">
		DELETE FROM community
		WHERE no = #{no}
	</delete>
	
	<update id="updateRecommend">
		UPDATE community
			SET
			<if test="recommend == 'recommend'">		
				recommend=recommend + 1
			</if>

		WHERE no = #{no}
	</update>
	
	<select id="getRecommend" resultType="Community">
		SELECT recommend
		FROM community
		WHERE no = #{no}
	</select>
	
	
		
</mapper>	



